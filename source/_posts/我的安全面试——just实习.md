---
title: "我的安全面试——实习"
onlyTitle: true
date: 2026-3-13 12:35:42
categories:
- 保密工作
tags:
- 保密工作
img: https://typora-202017030217.oss-cn-beijing.aliyuncs.com/%E5%9B%BE%E7%89%87%E7%B4%A0%E6%9D%90/1080P%20A%20%E6%94%B6%E8%97%8F%E9%87%8F%E6%9C%80%E5%A4%9A/1080PA%E5%A3%81%E7%BA%B8192.jpg
---



## 感觉比较好的面筋

我看过的一些面筋，可以引发我的思考，感觉有用的，每个问题都想一遍（当然二进制就别想了（：

https://sechub.in/view/2639447

https://github.com/vvmdx/Sec-Interview-4-2023?tab=readme-ov-file#0x00-%E5%AD%97%E8%8A%82%E8%B7%B3%E5%8A%A8-%E6%B8%97%E9%80%8F%E6%B5%8B%E8%AF%95%E5%AE%9E%E4%B9%A0%E7%94%9F

提前说明，本人自我介绍均会主动提起擅长java代审，其他方向不太行。如果是其他方向的web手（流量分析，php/go/python审计，渗透哥，src哥，ai哥），借鉴率可能没那么高，各位当个笑话看。

## 面试卡壳点

记录一些面试没答好的问题，持续更新

### 字节风控一面

算法，对数个ipv4进行排序，算法随意

没写出来，面试官说用冒泡排序写，然而我全忘了

总结下排序算法常用的思路，分治（分段排序，然后递归->快排），双指针（合并数组->归并），提出来一个元素找合适的位置加进去（插入），不合顺序的交换（冒泡）

* xss的防御：除了黑白名单，htmlescape这种，还应该提到CSP，他会用Content-Security-Policy限制只能加载本域某些js，而不会加载外来js，属于一种保底防御；并且csp可能还遇到jsonp跨当前域，白名单域调用jsonp接口，就会有函数回调。

* XSS 的危害，可能造成蠕虫攻击，在移动端上 XSS 可以调用 WebView 顶层接口，具体一下有没有可能触发 Chrome 的一些 0day 和 nday 漏洞。

  对于上述的防护，可以在采用 innerHTML、前端框架中的 dangerous html 方法等，也可以给研发提供一些更安全的 SDK，来支持转义和 JS 过滤。

* CSRF的防御：除了保底防御的CSRF token(服务器生成一个**随机 token**。用户请求时必须带上)，还应该提到Referer / Origin 校验，看下是否来自白名单，SameSite；还有验证码，二次确认等。原理都是浏览器只要请求目标域名匹配 Cookie 的 domain/path，就自动发送 Cookie，而csrf不在cookie内

* 微服务安全的全架构审计：应该提到网关、鉴权、服务间调用、RPC的事情

>1. **服务清单**
>
>  - 获取微服务列表（API 文档、Swagger/OpenAPI、服务注册中心如 **Eureka**、**Consul**）
>  - 标记入口服务（API Gateway、外部接口）和内部服务
>
>2. **接口和通信协议**
>
>  - HTTP/HTTPS、gRPC、WebSocket、消息队列（Kafka/RabbitMQ）
>  - 记录服务间调用方式、认证方式、授权策略
>
>3. **依赖和组件**
>
>  - 语言和框架（Spring Boot、Node.js、Go 等）
>  - 中间件（数据库、缓存、消息队列、外部 API）
>
>4. **身份与权限**
>
>  - 用户角色、服务账号、Token/JWT、OAuth2
>
>1. **外部接口**
>
>  - SQL/NoSQL 注入、命令注入
>  - XSS/CSRF/SSRF
>  - 不安全的文件上传/路径遍历
>
>2. **内部服务**
>
>  - 服务间未授权访问（Insecure Service-to-Service）
>  - 微服务暴露调试端口（Spring Actuator、/metrics、/env）
>  - 不安全的序列化（Java、Python、Node.js）
>
>3. **配置问题**
>
>  - 默认账户或弱口令
>  - HTTP 明文传输敏感数据
>  - 日志泄露敏感信息
>
>4. **依赖漏洞**
>
>  - 第三方库漏洞（CVE 检查）
>  - 不安全的容器镜像

* ssrf防御：黑白名单（前置WAF），可能会遇到dns rebinding重定向到本地的攻击，先解析后在做白名单也需要。

  * dns rebinding：浏览器同源策略不会持续验证域名解析后的ip是否变化。用ssrf指向攻击者自己的域名，攻击者设置极短 TTL，几秒后让该dns指向本地ip。流程如下，需要攻击者自己准备一个恶意dns服务器：

  ![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20260311192354218.png)



### 字节风控二面

原来是一面面试官太nice了

一面着重问思路，很明显是在业务上的，二面首先他就迟到了，然后进来也很吵，自我介绍完就给了一个很难的算法。中间我有语法问题，问他为啥跑不了，他说可以切到自己的IDE，我顺便拿去问了deepseek，结果思考了206s。首先对一个安全人员来说，既然要求算法我为什么学的是ctf不是acm？然后一个小时没写出来，之后他就不耐烦的问了10分钟的问题。我觉得他甚至没看过我的简历，直接问了常见的几个网安八股，xss的攻击和防御，csrf的攻击和防御，就没了。喂喂喂我写那么多java代审干嘛来的，叫我介绍之前经历最有意义的点，我提到绿盟设备对长数据包解析有问题，经典的案例就是shiro打有脏数据的哥斯拉webshell，他问了两遍什么shell？我说哥斯拉，ok没听过哥斯拉我没意见，为什么我回答后老是一两分钟都不说话，他一直在看自己上面的屏幕，除了哥斯拉基本一个字都没听我说，是只有哥斯拉这种奇怪的名字才能吸引他的注意力吗？

可能是我投错部门了，我投的风控——安全实习生。可能应该投安全攻防部，就没有这种面安全问算法的老哥了，我真没招了

说下题目吧，之后才知道是经典的算法题，但是哪个好人学安全刷算法的？

给定一个数 n，如 23121；给定一组数字 A 如 {2,4,9}，求由 A 中元素组成的、小于 n 的最大数，如小于 23121 的最大数为 22999

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/93a71157275b505b31a60a26878d3010.png)

还有比较有意义的一道题：

jwt相比cookie和session有什么优劣势？

优势：不用存储省服务器空间；可以放在header头，就能防止csrf

劣势：弱密钥或者密钥泄露会被长期伪造

ok这就是全场高光的一道问题。





### 腾子一面

好评，不刷算法，问的都是java核心，挺好的

实习经历挨着问，水货实习小心了

shiro反序列化，shiro反序列化怎么防？

我先说了jep290黑名单过滤一些危险类，然后他说如果机器很多，不能挨着改怎么办，我说那只能WAF了，想了一会儿我说可以限制header长度（我觉得能答出这个真的有点天才的，一个是真的有用，另外一个复现过shiro绕长度限制可以进一步引导面试官），他想了想，说不错，不过另外还有RASP，对啊，然后我讲了一大堆rasp

rasp的原理（随便讲），顺便扯到IAST，也是随便讲

反序列化的原理，几种反序列化的情况，就挨着说原生反序列化readObject/jndi/fastjson这种调用constructor,setter,getter的情况

为什么我的某个项目->指反序列化生成器为什么7个月没更新了😀，当时妹说写论文去了，笑嘻嘻就过去了

主要是介绍经历，问我360期间无聊，其他实习怎么样之类的。

有没有自己开发工具（说了给某公司写了个运维系统，然后分析过很多别人的工具之类的，bp插件？）

还问熟悉vibe coding吗？包熟悉的

整体没问什么技术，可能是自我介绍叫他去看博客和github了，感觉往这个方向走还挺好的，他会忙着去看项目，问也是针对项目问，当然是主场了。其他的都没怎么问，比如经典的owasp top10都没问，问我想不想写个算法，我说别搞，就没写

面试官答应一天给邮件回复，后续7天超时挂了，再被安全技术部捞起来问他为什么挂我，他说他也不知道。。

### 深信服一面

问了很多对抗的东西

webshell，内存马免杀（不会）面试官的意思说是自己搭个雷池绕，我就说了个overlong UTF-16

问了websocket内存马，绕反向代理，本来我想说Upgrade马，但是记不太清了妹说。现在看来答当前路径的通用回显、或者Upgrade马都比较合适，只要不新开映射都能弹回

缓存投毒（不会）

http走私原理，检测（只记得chunk了）

agent实践，氛围编程（说项目经验）

c2各种防检测，比如改配置文件等（不会）

可能是想问：

>使用HTTPS、DNS、WebSocket、CDN等合法协议传输数据。例如，将C2通信封装在HTTP/2流量中，模仿浏览器访问。
>
>利用**域前置**：通过高信誉CDN（如Cloudflare、Amazon CloudFront）转发流量，使真实C2域名隐藏在CDN之后，仅CDN与目标通信，目标看到的源IP是CDN节点。
>
>将C2配置（如服务器地址、端口、加密密钥）硬编码在二进制中时，使用加密或分段存储，避免静态扫描直接提取。
>
>使用“白加黑”技术，将恶意代码注入合法的签名进程中执行。



### 深信服二面

keborasting攻击

模型投毒、提示词注入手法

设计一个治理大模型隐私安全问题的方案

什么是联邦学习

什么是同态加密

介绍AI+IAST

银狐的原理和手法，应急

已oc，hr高高兴兴打电话来，但竹子已offer了，再见sxf

### 字节安全运营一面

感觉问的很深，每个都是问到不会为止，但是没有算法，666，全场景题，但是确实很有道理。心服口服

介绍运营事件整个是怎么判定的？（转文字原文：我们首先是去判断一个真实性，就是是不是误报这种就是看他的配漏的是不是有具体的攻击代码。比如说sql的，它肯定有相应的注入的内容，反序列化rce就肯定有runtime或者打字节码这种内容。就是看它是否与它的告警相符。如果相符，就是看他的payload有没有执行成功，然后我们当时是不能上机的，就比如说他搭配了的，比如说一个DNS log的外带或者是其他的之类的外联，我们就去看他有没有进一步的外联，或者说他打了webshell打了内存码，就去分析它这个内存码是否有。比如说listener内存码，它会去新建一个servlet映射之类的，我们就去看他有没有访问新建的那个映射。这样就进一步判断他有没有达成功，就是他去访问那个新的映射，可能会有执行命令，执行成功的回显之类的。至于加密的内存码就要进一步分析。比如说哥斯拉的加密的内存码，就要去匹配它的特征，要再进一步的分析。）

绿盟期间每天的告警量？会不会添加规则

大部分误报怎么处理（我说业务加白

你每天有上千条告警，那这些告警很多误报都是有共性的吗？

处理完之后，每周都会向客户去通报这个结果，对吧？这个是你去通报的吗？

比如说这个报告发过去，客户会有一些挑战，他们经常会问你吗？后期的处置是需要你你去做的吗？还是说你只要发现，向他告警就行了。

于是问到入侵排查

公司有一台内网的这个服务器，然后流量产品还有一条告警说有一个挖矿。好，这个是我们拿到了最原始的这个信息了，然后让你去处置你觉得你那你该从哪些方面去做？比如说从告警研判开始到确认威胁之后，要去上机排查，寻找这个攻击的源头以及最终止损、定损，写调查报告，你觉得你怎流程，你会去怎么做？

那其实说你比如说发现有一台机器挖矿让你去做，那我们可能关心的是他为什么会被挖矿，对吧？第二个他到底是挖了这一台，还是说挖了很多台？要去排查这台机器为什么会被挖矿，它是怎么打进来的。这个你一般的思路是什么？

好，如果你排查到了一台机器它的攻击源头之后，第二个那个横向排查，那你那你会去怎么排查？比如说我要意识判断他的他影响面是什么。第二个他会不会攻击？他攻击了这台机子之后，他会不会去做一些其他的事情，那这个该怎么做？

你可以再扩展一点，就比如说你从流量设备，因为你如果只有流量特征，你可能只局限于在流量特征上。但实际上你是可以上机去排查的，或者说这台服务器上可能会有一些其他的安全产品的日志的。就比如说主机安全的对吧？或者说从HIDS是的这个日志就你看可以做些什么事情。

能不能通过一些大模型帮忙处置告警

EDR，HIDS，有运营过类似这样的告警吗

怎么发现银狐的

你怎么发现这个微信上面有一些可疑的这种操作的？企业微信也好，我给你发一些附件，发一些东西，这其实也是比较正常的一个现象。那你你们为是怎么抓到这一次发送附件的方式是比较可疑的，然后才还有后面的什么分析附件，这就这样的这样的动作是怎么发现的？

我甲方的警惕性也没有那么高，或者说这个用户可能对安全也不是特别懂，对吧？他那这时候让我们去发现这个银狐事件，我们作为安全团队，那肯定也要去发现这样的银狐，对吧？那你觉得要从哪些方式去发现他？

内存马的查杀

内存马的实现原理

懂二进制吗

怎么治理龙虾带来的安全隐患，隐私泄露之类的

怎么用龙虾辅助安全运营？角色互换这一块

介绍完整的内网域渗透的经历



### 拓竹一面

我操，拓竹才是他妈的众望所归！真是遇到知音了，25min让我学到了很多有用的东西

java安全实战挖过哪些洞

JNDI注入？JNDI注入除了lookup还有别的sink吗？（答了各种getObjectInstance，包括今年alictf的hessian jndi，爽飞了给我，还打了LdapAttribute，总之lookup到getObjectInstance中间的如果其他方法有调用都可以一试）

JDBC attack?问了mysql和h2的，再次爽飞

布豪，讲完h2就问了HQL->hibernate，我真不知道hql注入了，学习！

问实战有没有挖过反序列化，好像没有（想起亿赛通，挖过来着），但打ctf还是自己挖过链子的，面试官此处不太满意

SSRF实战挖过吗？我太老实了，再次没有，遂问挖过哪些洞

逻辑洞，举了刚挖的云丹田

XXE/SSTI大量举例，包括python，java的，列举了一些->jinja,flask,thymeleaf,freemarker,vocility之类的

DNS重绑定（还好我字节被问过，这个手法确实没怎么打过，诚实脸

CSP？有什么用？（防XSS）

CORS，我操就记得叫同源策略了，面试官说拿来跨域的

jsonp？

http走私原理

Host头注入（再次不懂，面试官提示host碰撞，其实我都没听过这个洞

云原生懂哪些？metadata，特权容器

阿里云的元数据是什么？彻底露馅，此处面试官质疑到底有没有遇到过云metadata

云原生的api server

java反射怎么导致问题说一下，说了invoke和newInstance..

你觉得你比较擅长什么？再次java代审

go审过哪些洞

接触安全多久了？为什么每个面试官都这么问我

答上了70%，不过实战太少，但是专业对口，期待！

（后续进二面了

### 拓竹二面

实战审过那些大型复杂的漏洞？

有没有挖过开源组件的漏洞

有没有自己挖漏洞（代审）的方法论？于是疯狂讲sink溯源之类的

sink点被封装在工具或者定时任务，这种web上没有入口，大型项目下有很多这种情况，怎么审（经过h神的提示，原来面试官是想问微服务项目，微服务架构除了在单个服务内追踪 source 到 sink，还需要关注 RPC 接口、消息队列等服务边界，将其视为新的 source 或 sink，我不知道这里该不该提起mscan和污点分析，因为用的比较少。。

```java
1️⃣ RPC接口（HTTP / Dubbo / gRPC）
@PostMapping("/xxx")
public void api(@RequestBody Input input)
2️⃣ MQ消费者
@KafkaListener
public void consume(String msg)
3️⃣ 定时任务
@Scheduled
    都是作为新的source
```

有没有审计过大型商业项目（泛微，帆软

稍微大点的项目怎么审（说到codeql跑别人的规则，tabby自动化，实际搭起来对应的功能和代码熟悉一下，包括鉴权和安全机制，第三方依赖，感觉答得很行吧，但是面试要求实战有这种经验

说一下java已知的第三方组件，框架都有些什么漏洞，知道多少说多少（疯狂输出，讲完fastjson,jackson嘴都麻了，面试官说先不说fastjson,jackson了，然后把其他的resin,rome,spring aop,hessian,dubbo,snakeyaml,rhino等一大串全部说了一遍，感觉面试官还没听够，说到后面我好多想不起来了，惭愧

resin有什么特性？（可以用java跑php，刚问的gpt

shiro 550，721有没有实战打过？721打密钥的条件（真记不得了，因为利用条件比较苛刻，看过一遍就再也没遇到了

除了550，721，shiro还有什么打法？看过好几次权限绕过，就是答不上来，曹，再次惭愧（面后看了是shiro和spring解析差异的问题

知道spring security吗（不知道

bypass RASP的姿势？说了反射调到native之间的栈，可能RASP没过滤完。或者读源码下来，RASP可能直接在代码里有开关，把它关掉。在一个就是自己编译so加载。此处面试官没听够

反问阶段问RASP还有什么姿势？因为确实很想知道，说是还能新起线程注入，新线程可能不会被hook？但是每个request不都是新线程吗，此处不懂，后续试试

jetty知道吗？有什么特性？有什么问题？不知道，只知道jetty的内存马的构造，对比其他中间件的特性真不知道

codeql和tabby有没有自己写全套规则？没有，都是八别人的来用或者有单独的链子要找，找ai写，并没有全套规则去审一套系统

jdbc反序列化？挨着说，知道多少说多少

挖的最深，印象最深刻，利用最复杂的一个漏洞？想不起来了，讲了ascii-jar打postgresql，overlong utf-16，虽然没那么复杂，但是比较通用

（进终面了



### 拓竹三面

boss面

挖过最难的洞是什么

vibecoding看法

根据最难的洞再问一些技术问题，此处已无参考性

对之前的实习工作和公司有什么看法

拿了些什么奖，打过最难的ctf题是什么，依旧根据题目再问两句

已oc



### 另外我觉得有意义的一些问题，不过面试没人问，可能觉得我渗透太菜

如何绕CND找ip（这个问题我一直不知道答案，可能是方法太多了）

CDN（比如 Cloudflare、Akamai）的作用是**隐藏源站 IP**

子域名泄露（最常见）很多站只是主站走 CDN，但子域名没走； 历史 DNS 记录；邮件服务器 / MX 记录

域前置？





