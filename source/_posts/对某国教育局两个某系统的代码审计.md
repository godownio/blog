---
title: "对某国教育局两个某系统的代码审计"
onlyTitle: true
date: 2025-9-31 23:59:59
categories:
- 保密工作
tags:
- 保密工作
img: https://typora-202017030217.oss-cn-beijing.aliyuncs.com/%E5%9B%BE%E7%89%87%E7%B4%A0%E6%9D%90/1080P%20A%20%E6%94%B6%E8%97%8F%E9%87%8F%E6%9C%80%E5%A4%9A/1080PA%E5%A3%81%E7%BA%B8174.jpg

---

红队打进去直接扒下来的两套代码，由于并不是源码，所以都不能调试，怀疑有漏洞都在线上找的靶机

当然因为我是彩笔的原因，一个系统只找到一个受限的反序列化，一套后台的SQL注入，都没有什么利用空间，各位可以不带脑子观看的一集

但是从拿到这种大型项目束手无策，到上手初步审计完整个项目的思路，完全对其他项目也适用。

虽然我看这两个把我心态审崩了，入职多年的同事也同步在审，和我一样没什么收获。工作后的审代码，也许就是不断的自我怀疑。我这种急性子，也许应该找个字节码插桩开发？或者根据漏洞写写poc？而不是从0到1的挖一个系统的漏洞，一直挖不出东西会感觉怀疑人生，感觉自己是个fw

# destiny

安装后部署jar所在路径：Follett\destiny\wildfly\standalone\deployments\destiny

有destiny.ear，以及两个war文件

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017143246252.png)

* destiny.ear/ ：被解压的 EAR 企业应用（适合手动部署）
* destiny-api-war-1.3.0.war ：API 后端服务
* destiny-console.war ：前端控制台服务

这些文件/文件夹的存在是因为这是 JBoss AS 的 **默认部署目录**，放进去后，部署扫描器会根据配置（自动或手动）来决定是否部署它们。

首先是console.war，管理网站web应用的，查看xml如下，只允许本地访问，所以忽略这个

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017143406979.png)

然后是api，随便找了一个，一般都开放了api

jboss-web.xml看到路由为/api/v1

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017143426295.png)

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017143433789.png)

先审api系统

## 环境

rhino-js 1.7R2，有反序列化漏洞（这下知道你学的反序列化漏洞有什么用了叭

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017143557822.png)

先读配置文件standalon.xml，这是jboss最重要的映射配置文件

用的h2database，默认账号密码为sa/sa

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017143621549.png)

用的服务器是Undertow ，我看依赖也是有Tomcat的

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017143634421.png)

管理接口和监听接口同样默认只能本地访问

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017143645247.png)

dashboard路由/dashboard/servlet/presentconfiguredashboardform.do

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017144249660.png)

## 分析

我这次分析只用了sink搜索工具和jadx

本项目不存在源代码，项目也切分为了多个业务jar包，从逻辑上来说分为：

jar1.jar jar2.jar ....这些都是业务代码，而不是一个jar包

这些业务jar包统一被放在lib库下，jboss启动时会自动的读取和配置jar包

我的操作是：

把lib下各个名字的jar包，怀疑是第三方组件的就删去，比如cc，shiro这种，直接删。因为我们审计sink肯定不会审第三方组件的0day，同时记录下这些组件的版本号，jar包上没标就去各种xml下面找。同时不认得的jar包，也不知道是不是业务代码的，拿着名字去maven上搜。搜到就代表是第三方组件，删！

jadx可以批量反编译jar包，把jar包放到一个文件夹，然后直接反编译文件夹就行，jadx会自动整合项目目录

导入idea用springkill的sink搜索工具（需要导入config key）挨着审计从sink到路由，看看是不是用户可控的

中间可能会遇到各种路由映射和反射调用Endpoint的问题，遇到了再看

### 反序列化（可验证）

先把唯一一个有漏洞的贴前面

RemoteFileReplicator的run方法，从serverSocket中读取序列化数据并没有任何过滤的反序列化

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017145337281.png)

跟进构造函数中，socket port从系统变量里查 "FAILOVER_FILE_REPLICATION_PORT" ，如果没有值默认为50000

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017145421533.png)

在看哪启动这个run的

FailoverService.start()的一个链式调用

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017145449935.png)

这里有个if，需要系统处于passive后者active才会进入

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017145510073.png)

**检查系统是否运行在** **Failover** 容错环境中。

active **模式** → 当前节点正在提供服务

passive **模式** → 当前节点在等待接管（备用机状态）

其他模式（比如 "standalone" 、 "disabled" 等） → 不算容错

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017145524458.png)

根据下面的DestinyStartup.startup启动过程，是一个典型的 **Java EE/EJB** **单例启动类**（ @Singleton @Startup ），意味着 **应用一启动就会执行** startup() **方法**。

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017145617242.png)

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017145623662.png)

按理说应该扫端口，扫到50000端口就能直接打rhino反序列化

https://boogipop.com/2023/05/01/Mozilla%20Rhino%20%E5%8F%8D%E5%BA%8F%E5%88%97%E5%8C%96%E5%88%A9%E7%94%A8%E9%93%BE%E5%AD%A6%E4%B9%A0/#Rhino1%E9%93%BE

但是简单推测其功能，根据RemoteFileReplicator远程文件复制， **Failover** 容错环境。这就是个容灾功能

我也在struts-config里找到了容灾诊断页面

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017145805190.png)

经在线测试，这个页面未授权也能访问，如果在active和passive处有ip显示，就是开了容灾，就能打

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017145822838.png)

payload只需要链接对应端口，发送rhino序列化流即可

然后随便点点发现这是个静态页面，必须在系统环境里设置如下变量为true

FAILOVER_FILE_REPLICATION_ENABLED

standalone.conf里设置JAVA_OPTS="$JAVA_OPTS -DFAILOVER_FILE_REPLICATION_ENABLED=true"才行

这就是本系统代审唯一的收获了



下面从头开始介绍，这个系统的Endpoint转交和struts2映射还是挺有意思的

以下均为无法利用，随便写写的分析

### getter调用

JSONHelper.convertBeanToJSON会触发任意类的getter调用

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017150115514.png)

convertObject会调用到convertBeanToJSON

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017150128393.png)

寻找调用到convertObject的地方，看到传入的参数来自EJB.lookup，参数固定，所以这里不能利用

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017150141070.png)

### SQL注入

#### SQL1

跟进到这个VopacFacadeBean.getVopacTitleDetails，看到可以被sql注入

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017150210606.png)

getVopacTitleDetails用来根据一本书的 bibID 获取它的详细信息，返回一个TitleDetailsVopacValue 对象，后面需要传输才用convertObject转成json，很明显是没办法控制

跟进到VopacFacadeBean.lookup，jndi注入特征

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017150226164.png)

但这里lookup的参数固定

不过可以继续跟一下sql注入的点

没有任何过滤，来自ControllerURL进行的sql注入

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017150240952.png)

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017150252841.png)

不过这里也是分析出了Endpoint的传参方式

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017150407435.png)

#### SQL2

TitleAEDBean#getSiteBibStatus

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017150527095.png)

可以看到也是能注入的，而且前面没有任何处理，但是在调用这个函数之前，都会调用到getTitleInfo去查找bibID，不能绕过

### RCE

执行了ping + address

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017150606792.png)

跟进发现参数固定为几个ip，根据名字可以看出是监控面板

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017150617261.png)

### 身份认证和路由

找了半天，找不到路由

destiny.war/struts2-config.xml中，根据主页有的路由，搜索到action块指向了这个路由，可以看到struts2框架对path和对应的类GenericHandleAction做了映射

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017150714619.png)

（有部分xml不会被反编译出来

看到GenericHandleAction只是继承了Action，头上没有Controller，说明有没有Controller无所谓，在xml里做了映射就行

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017153556111.png)

struts映射的关键点就在于继承Action，只要继承了Action，再在struts-config.xml中做映射，就能加上.do暴露给web端

比如要访问questwelcome，对应的映射为/quest/servlet/presentquestwelcomeform，按照前面的规则，还需要加上.do，结果为/quest/servlet/presentquestwelcomeform.do

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017153700894.png)

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017153715113.png)

也是可以成功访问，但是需要授权

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017153737228.png)

这里有两个问题，为什么要加上.do？为什么jsp里没有权限限制，但是仍然需要登录?

#### 为什么要加上.do？

需要加上.do的原因是web.xml.template里配置了以.do结尾的才会交给struts处理，也就是走struts2-

config.xml映射

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017153800946.png)

注意到这个配置是在web.xml.template中，这是个什么文件？有时候可以当成web.xml去看，有没有具体使用可以用环境去测试

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017153815328.png)

#### 为什么jsp里没有权限限制，仍然需要登录？

web.xml.template误打误撞看到了全部的映射

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017153846820.png)

都是能访问的，不能访问会显示404而不是xml error

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017153858397.png)

往上翻，可以看到访问action，jsp等都需要经过LogFilter-Servlet，所以会记录日志

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017153910437.png)

最重要的是OAuth-Login-Filter会去验证登录权限，可以看到rest路径也是要身份认证的

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017153921680.png)

filter对应的类如下

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017153931416.png)

看一下身份认证的逻辑，com.follett.fsc.destiny.client.common.oauth.OAuthLoginFilter.doFilter中，从Session中取出siteID（用来决定用户属于哪个学校/馆），如果没有，转到doLogin

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017153946360.png)

如果 token 无效（或者被吊销），返回 **401/403** 错误。

如果当前 Session 没有登录，或者已登录的用户和当前 token 对应的 userID 不一样，就强制调用 EJB登录。

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154002066.png)

先根据userID去调用findByPrimaryKey查找user，然后调用loginUser登录

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154011944.png)

调用到sql的部分省略，直接看到如下sql，调用的appendSafeSQLLimiter，应该不会有注入漏洞

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154022893.png)

/rest/v4/catalog/holdmultiple?contextName=district&site=&bibID=12345&canHoldMultipleCopies=true&quantity=2

根据上面的认证，可以看到访问rest也是要从session取出siteID才行的，所以我在尝试注入时才会报错

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154042651.png)

### commandline路由

看到commandline路由对应了com.follett.fsc.destiny.session.cmd.CommandLineServlet

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154112931.png)

该servlet下有doGet和doPost请求，pareRequestParameters解析了除了command之外的参数，command是单独分离出来的参数，然调用processGet去处理，doPost差不多，只是在前后加上了saveFilesToUploadDirectory和secureFileDownload

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154150444.png)

进去会创建一个CommandLineSession，然后调用launchCommand

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154430716.png)

跟进launchCommand，先是调用了loadCommandClass

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154442354.png)

跟进loadCommandClass，如果command中不包含. 则前面拼接全限定名 "com.follett.fsc.destiny.session.cmd.impl." ，如果前面包含. 则直接去loadClass并实例化

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154500053.png)

比如我去command实例化UpdateClasses

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154516831.png)

也就是说这里可以实例化任意类的构造函数，但是只能传string进去了

而且这里后续需要继续利用的话，可以看到newInstance后被强转为CommandBase类型，而com.follett.fsc.destiny.session.cmd.impl.下的所有类都继承了CommandBase，其他包下的却没有，所以要顺利运行到后面都需要传这个包下的类

继续看传参的部分，调用了mapAndSetIncomingParameters

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154540343.png)

跟进，反射调用了被@CommandParameter注解的setter方法

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154553901.png)

随便点开一个com.follett.fsc.destiny.session.cmd.impl.包下的类，只需要传 字段=参数值 就能调用其setter

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154605681.png)

再次回到launchCommand，完成了设置参数后，会调用其validate方法，并启动线程

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154619422.png)

启动线程也就是调用execute方法了

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154626959.png)

所以我们可以控制该包下的 任意类的validate 和execute方法及参数，找一下这个包下面的execute有没有利用空间

但是我在线上测试的时候老是报版本不兼容？？

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154643250.png)

### form任意方法调用

GenericAjaxAction的execute方法中，如果form表单是GenericForm的子类，则验证权限后调用executeAjaxMethod，要调用的方法名为ajaxcall传参进去

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154718338.png)

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154723563.png)

跟进，发现直接是反射调用，不过必须是无参的，如果有可利用的GenericForm的无参方法，可以考虑在此调用，不过需要有session

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154737929.png)

### 反序列化2

CacheSerializer

直接反序列化了，但是没找到调用

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154806497.png)

### Endpoint注解方法调用

DiscoveryAgent.getEndpointMap()如果方法上有 @Endpoint 或 @Bootstrap 注解 → 就注册成一个EndpointValue 

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154836571.png)

跟进getListOfClassFilesInQuestPackage，发现只会加载com/follett/fsc/destiny/client/quest/helper下的类

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154848210.png)

ev.getKlass() 就是对应的 Quest 控制器类（例如 HoldController ）

每次请求新建一个实例，注入当前的 store 和 request 

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154906787.png)

ev.getMethod() 是当初扫描时发现的 @Endpoint 方法

parseArgumentsFromRequest(m) 会根据方法签名，从 HttpServletRequest 里取 query 参数并转换类型

m.invoke() 直接执行方法，相当于动态路由

接着看到QuestAjaxAction，这是一个 Struts 1 的 Action 类：

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154940108.png)

请求 URL 会先经过 Struts 配置 ( struts-config.xml )，被路由到这个 Action。

对应的表单类 ( ActionForm ) 会被实例化并传入 execute 。

如果是 QuestDAO，会直接调用我们之前分析过的 processRequest()。

也就是说：

**请求** **→** **Struts** **→** **QuestAjaxAction** **→** **QuestDAO** **→** **processRequest** **→** **Endpoint Method**。

接着去struts-config.xml找到路由

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017154952494.png)

加上.do，就带出了返回的json数据

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155001612.png)

看到是有三个QuestAjaxAction，其实name不同对应的ActionForm也不同

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155012305.png)

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155024151.png)

这下终于知道quest下的Endpoint怎么使用的了

比如之前死活注不进去的HoldGranter

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155039481.png)

这样就能访问了

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155050830.png)

### 反序列化3

com.follett.fsc.destiny.bcconnector.impl.DataConnectorImpl

这个类由@Path注解

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155142714.png)

这里面的方法，都会去调用一个movePayloadToPayloadCollection_intarray

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155152076.png)

该方法会去调用DataObject.unmarshalIntArray

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155202502.png)

unmarshalIntArray毫无过滤，直接对传入的base64解码并反序列化

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155215183.png)

继续回.DataConnectorImpl下的方法，看一下给movePayloadToPayloadCollection_intarray传的参数

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155224882.png)

getInterface一直跟进，可以看到new了一个ClientInvocationHandler

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155235037.png)

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155243384.png)

这个类继承了InvocationHandler，典型的动态代理，那调用这个类的方法都会转到ClientInvocationHandler的invoke方法，代理的类是最开始传进去的DataConnectorInterface.class

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155256866.png)

这个类的invoke方法把本地方法调用转成远程 HTTP/WebService 请求（client proxy），先是经过了一遍Interceptor（这里我没截图），然后通过processRequest发起请求，并把结果经过unmarshal反序列化

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155312245.png)

这里发起请求的请求需要config开启allowAutomaticRedirectProcessing，通过ClientRequest发起请求

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155324571.png)

可以很明显看出是发起请求了

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155335768.png)

综合看来该功能为：

**向远程身份认证服务请求**，获取与某个 contextToken （上下文令牌，可能是登录凭证或会话 token）关联的 **一组授权** ID。

过程中会：

1. 给请求加上签名头（保证请求来源合法，防止篡改）。
2. 调用远程 REST API /bc/{token}/authIds 。
3. 把返回的 Base64 编码的 int[] 转成人类可读的 List<String> 。

只是这个Host port都不能自己设定，这应该是RESTAPI的功能，看一下rest包

还真是，刚好对应上

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155356048.png)

看来就是一个web 和rest关联的功能点，进行身份认证的时候动态代理默认向rest api发送认证消息

代码太复杂了这里，但是看功能应该没问题，因为没什么可以控制的点，除了token

而且这里反序列化是反序列化rest api返回的数据，token还是得丢过去校验的，没什么利用空间

### **SlaveManager** 任意文件写

这是除了容灾部分反序列化外，最有希望的一个洞，但是功能又又又不是默认开启

SlaveManager的run方法中，开启了1059端口，然后绑定到了一个PeerToPeerSocket上，然后

然后启动RequestHandler.start，也就是调用RequestHandler的run方法

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155527529.png)

转到RequestHandler类，这是个SlaveManager的内部类，调用了PeerToPeerConnection的构造函数

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155538850.png)

可以看到对字段peerSocket赋值后调用了start -> run

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155553538.png)

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155600877.png)

PeerToPeerConnection调用Command.read

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155612221.png)

跟进，可以看到全是从socket中取值，根据readLong读取的值，进入不同的case

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155626206.png)

其中FILE和JAR都是写文件，比如readFilePayload，写到临时目录下，结尾为.temp

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155637618.png)

这种写临时文件的理由空间不大，但是case为FOLDER的时候，处理就不一样了

可以看到路径是自定义的，而且上传zip之后，还会自己unzip出来，任意文件上传

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155649406.png)

可以看下是怎么unzip出来的，调用cmd去执行jar xf解析压缩包

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155702229.png)

可惜经过了getCanonicalPath，不能直接命令注入

这个向节点分发文件，也是要手动调用一个SendCommandsToSlave main函数才能使用



# PowerSchool

这个系统大一点，毕竟上面一个是小大小闹的图书馆系统，一般在旁站，powerschool是个学校的主系统

部署好exe后查看deployment-agent-webapp-xxx 下的menifest发现部署的jar都来自WEB-INF/lib下

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155802804.png)

另外还可以看到路由分发是在springWebDispatcher-servlet下，web.xml也是

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155812006.png)

直接everything找到

```xml
<?xml version="1.0" encoding="UTF-8"?>
<web-app id="Inst_Console" version="2.4" xmlns="http://java.sun.com/xml/ns/j2ee"
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
xsi:schemaLocation="http://java.sun.com/xml/ns/j2ee
http://java.sun.com/xml/ns/j2ee/web-app_2_4.xsd">
<display-name>Deployment Agent</display-name>
<context-param>
<param-name>contextConfigLocation</param-name>
<param-value>classpath:/agent-application-context.xml</param-value>
</context-param>
<listener>
<listener-class>
org.springframework.web.context.ContextLoaderListener
</listener-class>
</listener>
<servlet>
<servlet-name>CXFServlet</servlet-name>
<servlet-class>
org.apache.cxf.transport.servlet.CXFServlet
</servlet-class>
<load-on-startup>1</load-on-startup>
</servlet>
<servlet-mapping>
<servlet-name>CXFServlet</servlet-name>
<url-pattern>/cxf/*</url-pattern>
</servlet-mapping>
<servlet>
<servlet-name>springWebDispatcher</servlet-name>
<servlet￾class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
<load-on-startup>2</load-on-startup>
</servlet>
<servlet-mapping>
<servlet-name>springWebDispatcher</servlet-name>
<url-pattern>/spring/</url-pattern>
</servlet-mapping>
<filter>
<filter-name>authenticatedSystemFilter</filter-name>
<filter￾class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
</filter>
<filter-mapping>
<filter-name>authenticatedSystemFilter</filter-name>
<url-pattern>/*</url-pattern>
</filter-mapping>
<!-- OAuth2 resource filter to intercept any requests for remote API
invocation -->
<filter>
<filter-name>OAuthResourceFilter</filter-name>
<filter￾class>com.pearson.platformj.webservice.oauth.ResourceEndpointFilter</filter￾class>
<init-param>
<param-name>noAuthURLs</param-name>
<param-value></param-value>
</init-param>
</filter>
<!-- OAuth2 resource filter to intercept any requests for rest api
resources -->
<filter-mapping>
<filter-name>OAuthResourceFilter</filter-name>
<url-pattern>/cxf/v1/server/*</url-pattern>
</filter-mapping>
</web-app>
```

可以看到大部分都是身份认证的映射，处理请求的是spring默认的Dispatcher和Listener

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155847871.png)

既然部署后的 JBoss（或 WildFly）系统使用 .exe ，读取 MANIFEST 并动态加载上百个 JAR 包，我直接把这些jar包批量解压出来，不然根本审不了

根据agent-application-context.xml，发现配置文件位于RuntimePropertiesServiceImpl，另外还会自动对com.person.platformj下的包注入依赖

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155905730.png)

根据这个读配置文件

>从系统属性 com.pearson.pss.installer.deployment.runtimeproperties.dir 读取配置目录路径，如果存在则直接返回。如果上述系统属性不存在，从系统属性InstallConstants.PROPERTY_KEY_INSTALL_BASE （安装根路径）读取。
>
>然后在该路径下拼接 "configuration" 目录，返回该目录的绝对规范路径。

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155932329.png)

于是找到PowerSchool下的configuration路径

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155943426.png)

deployment配置了安装时输入的用户账号和密码（db的配置也是这个逻辑），services是tomcat的基础配置文件

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017155952243.png)

审了半天，发现这部分是deployment，部署平台。。

环境：jdk1.8.302 + Tomcat 9.0.35

有JNDI的话可以打高版本绕过

activeMQ 5.16.7 安全

## 分析

### 反序列化

随便找了个反序列化的sink点，发现套了白名单

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160040506.png)

查看白名单，加载的multicastmessagedispatcher

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160104586.png)

跟进到ObjectWhiteList，定义了四种白名单，去类路径object_whitelists目录下xxx.list就是白名单

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160118457.png)

比如muticastmessagedispatcher只能反序列化如下

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160128441.png)

### SQL

#### SQL1（已验证）

这是个注入点，不过需要登陆上去

com.pearson.powerschool.app.teacher.TeacherClass 360行直接拼接，这种直接拼接后面用的预编译也会有sql注入吧

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160154230.png)

向上跟进到com.pearson.powerschool.xte.resources.UserResource，在这个类进行了登录验证，其中teacher/login路由调用到loginService.login

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160202217.png)

跟进到com.pearson.powerschool.app.teacher.LoginServiceImpl，在这里转到com.pearson.powerschool.app.teacher.TeacherClass

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160213985.png)

回到sql注入那，psmSectionId来自loginTarget3

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160249600.png)

loginTarget3 的值来源是（逻辑在GPV.GPV）：

* 先在 `ProcessState._apostname` 里查找是否存在 "loginTarget" 。
* 如果存在，就取对应位置的 ProcessState._apostvalue 。
* 如果不存在，就取默认值 "" （空字符串）。

整体分析TeacherAccess的逻辑， loginTarget3 ：从 GPV("loginTarget") 拿到的目标 URL。在用户登录成功后，检查是否需要强制重定向（ forceRedirect ）。如果需要，并且目标 URL( loginTarget ) 包含 psmSectionId 参数，就去数据库查找该 section 对应的 schoolId ，以便后续跳转或权限控制。如果目标 URL 里包含 psmSectionId 参数，说明用户要进入某个 **课程** **Section** 页面。

这个section的地方是能注入的，且要在认证成功后跳转的地方注入

可惜我目前没办法身份认证，也没本地环境测试

多嘴说一句，中间怕又审错框架了，然后找到了路由

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160340536.png)

不过除了public，其他路由输了直接跳谷歌登录了，明显的身份认证

### 身份认证

GetNewUserArr在没有找到Session的情况会调用constructSessionState()去创建session

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160400521.png)

如果这里有session，则会根据session中的字段使用不同逻辑获取用户信息：

* teacher/staff ：通过 TeacherCacheItem.lookupById()
* parent ：通过 GuardianAccountManagementService 和 PCASContextFactory
* student ：通过 StudentCacheItem 或 PremierStudentService

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160420410.png)

那么看下怎么创建session的，跟进到constructSessionState()

前面是装填的IP 地址、浏览器类型、平台、User-Agent、X-Forwarded-For，这些都能伪造，但是后面的三条认证相关（认证结果、服务类型、密码过期时间）就不行了

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160434287.png)

跟进到装填认证信息的地方， PCAS_AuthenticateUser_direct（PowerSchool CommonAuthentication Service）来认证用户登录，看参数也知道

username → 用户名

pwhmac → 加密后的密码（HMAC）

service → 认证服务类型（例如家长门户、教师门户等）

ignoreResets → 是否忽略密码重置要求

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160449740.png)

然后去看svc.authenticateUser的实现逻辑

锁定到了认证的主逻辑：

com.pearson.pss.pcas.sdkimpl.CommonAuthenticationServiceImpl#authenticate

首先是用队列去装认证，认证次数过大会Lock账户

AuthenticateQueue.delayInQueue(ownerServiceName, userName, sourceIPAddress);

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160506851.png)

然后还有记录登录历史之类的，实际认证的还是调用的一个AuthenticationHandler(authHandler.authenticate)去认证的

实现AuthenticationHandler接口的也只有AuthenticationHandlerImpl

写一下认证流程：

首先是判断force_authentication，开启了这个选项后面会有密码过期后强制修改密码，和密码复杂性判断

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160524245.png)

然后是通过account.findByUSerNameAndService查找数据库里有没有这个userName，以及判断

password的输入是不是空之类的基本判断

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160532561.png)

另外通过account.getServices去获取账号是属于什么services，比如教师就应该是teacherServices，账号必须绑定至少一个服务

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160544754.png)

如果 账号在数据库中的service和方法传入的参数services（应该是用户访问的service吧）不对应 且不支持 SSO，则拒绝访问

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160553001.png)

SSO 的核心思想是： 👉 用户在 **一个系统** 登录成功之后，可以在 **多个关联系统** 之间无缝切换，而不用每个系统都重复输入用户名和密码。

然后就是validateCredential去查证账号能不能对得上密码，红框的下一个if是判断这个账号是不是被激活过，激活了的才可以

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160612581.png)

也就是TM的，是先从数据库查了有这个用户，且有其对应的服务（比如他是个学生就对应studentService服务），才去验证密码。这种情况我连validateCredential都不用看了，手上连用户都没有

哎还是贴一下吧

validateCredential的参数里，crendentials凭证是个Map，包括了密码的加密方式（ ClearText 、SHA256 、 HMAC 等）和一个字符串。如果是ClearText，也就是明文密码类型，调用compareCleartextToStored ，拿明文密码和数据库里存的加密密码比对。

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160632748.png)

如果没有明文密码，就挨个检查凭证 map 里的其他加密类型。调用 validateCredentialWithType ，用不同加密方式来验证用户输入和数据库存储的是否匹配。

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160648451.png)

数据库中拿来比对的密码来自于之前的AccountDAOImpl.findByUSerNameAndService，如下，用的参数绑定，不能sql注入

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160700343.png)

而且我看了这个DAOImpl，他的sql语句全是用的参数绑定的方式，可以排除登录的地方sql注入了这个认证方式是基于有这个账号才去认证，那在哪里注册的这个账号呢？现在暂时看不到

### RCE

invoke里面调invoke鉴定为都不用看，向上找入口效率太低了。看到一个非invoke调invoke可以看一下，比如某checkAxxx调用了invoke

### 构造函数和setter调用

com.pearson.powerschool.cache.AbstractCacheImpl#put

这里调用了doCacheInterceptorBeforePut

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160921100.png)

跟进到doCacheInterceptorBeforePut，如果 cacheIdentity 存在且isCacheInterceptorEnabled() 为 true ，则调用cacheInterceptor.onBeforeGet

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160932609.png)

继续跟进，如果传入的参数为Collection或者Object[]子类，则调用generateCopy

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160943018.png)

三个类型的generateCopy都会调用到doGenerateCopyToPersist

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017160959424.png)

继续跟进doGenerateCopyToPersist，这个函数内先是对传入的Object entity调用了ReflectionUtil.createInstance，也就是反射调用构造函数创建类，并且如果目标字段被OneToOne注解，则调用ReflectionUtil.setValueByField反射调用setter赋值

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017161014115.png)

也就是说调用com.pearson.powerschool.cache.AbstractCacheImpl#put时，如果没有被cache缓存，会把对象拷贝一份，拷贝的方式是去调用构造函数和setter赋值

不过只能调用OneToOne注解的字段的setter，但是这里任意对象的无参构造是能任意调用的，但是没有defineClass的情况下不能加载自定义的传入类，似乎，没有利用方式？如果有可控的反序列化点，可以调用这个put去触发整个链子，调用到put的地方还是挺多的

比如这个项目虽然用了已经被修复的cc，不过链子依旧可以到达put。可惜，根本没有反序列化入口

### 二次反序列化

com.pearson.powerschool.milan.sdk.common.CopyUtil#deepCopy

直接进行了序列化后反序列化，没有过滤，二次反序列化的点

![](https://typora-202017030217.oss-cn-beijing.aliyuncs.com/typora/image-20251017161136222.png)

但是没看到有调用这个deepCopty的地方



## 工作笔记

另外附加一些师傅教的打国外网站的信息收集过程

渗透笔记：

dnsdumpster里通过ASN等信息判断其c段，区分内网段、混合云、虚拟主机

混合云的organization是microsoft、tencent cloud之类的，且很多个主机都是在云上

虚拟主机就是零散的

结合virustotal收集历史dns解析记录和其他信息，与dnsdumpster配合使用

收集到很明显的属于其c段的到fofa/shadon里查看存活主机，看organization看i直接判断（certificate里的Organization可以直接确定）

fofa里是有直接的端口的，一般不用批量去扫全部端口。但是扫到的ip可能不是主机，而是防火墙，这时候就需要去批量扫端口了，不过还没给我说怎么判断防火墙。。。

fofa certificate里CommonName:*.multicare.org这栏可能是主机名而不是子域名

漏洞扫描用nuclei去扫，目录用dirsearch就可以

如果没扫出东西，就需要挨着手测

### 工作问题

win10进去后直接修改网卡为192.168.100.0网段下，虚拟机开桥接（不复制物理机状态）即可Ping通代理

手动下载vmtoolshttps://package.vmware.com/tools/releases/lateset/windows

kali全局代理：https://fushuling.com/index.php/2024/12/29/linux下设置全局代理/

clash linux需要以普通用户启动，安装完tun服务后需要重启

另外，搜索kali配置静态ip

kali记得要配dns！而且是在配置文件里面配，而不是在clash里面，不仅要配阿里云腾讯云，还得配谷歌的，因为本代理是国外的，访问国内dns有概率被墙

浏览器要选Use系统代理

输入法用的ibus，配完需要重启电脑

2024.03 jet系列能直接用https://github.com/zpj80231/idea-set/blob/master

且要以普通用户打开，不然项目在root下权限不够会闪退

cs：kunkun cs

yakit也是需要普通用户打开

似乎这些UI程序都需要普通用户？

截图用flameshot，键盘设置里添加快捷键flameshot gui

fofa用临时邮箱登录，对很多邮箱域名已有检测，不过用awamail.com上的临时域名还是注册上了，看来黑名单不行

保密怎么通信！直接用国外的飞书，临时手机号登陆

my-yso linux运行需要ctrl shift R全局替换calc为mousepad，避免groovyc预编译出错

